# 휴멘드 HR — 인력파견 매칭 플랫폼

## 1) 포트폴리오 소개

인력파견 업체를 위한 **올인원 인력 관리 플랫폼**입니다.
파견 근로자(회원)와 파견업체(관리자) 두 역할을 지원하며, 채용공고 등록부터 지원/배정/출근확인/급여/계약까지 인력파견 업무 전 과정을 디지털화합니다.

- **웹**: https://humendhr.com (Next.js 16, Vercel 배포)
- **Android 앱**: Capacitor 하이브리드 앱 (Google Play 배포)
- **iOS 앱**: Capacitor 하이브리드 앱 (TestFlight/App Store 배포)

### 기술 스택
| 분류 | 기술 |
|------|------|
| 프레임워크 | Next.js 16 (App Router, Server Components, Server Actions) |
| 언어 | TypeScript (strict mode) |
| 인증/DB | Supabase (Auth + PostgreSQL + Storage + RLS) |
| 스타일링 | TailwindCSS v4 + shadcn/ui (Radix UI) |
| 모바일 앱 | Capacitor 8 (Android + iOS 하이브리드) |
| 파일 저장 | Vercel Blob Storage |
| 아이콘 | Lucide Icons |
| 기타 | date-fns, recharts, jspdf, signature_pad, tiptap, resend, @dnd-kit, googleapis |

---

## 2) 작업 범위

### 회원(파견 근로자) 기능
- 회원가입/로그인 (전화번호, 구글 OAuth, Apple 로그인)
- 채용공고 검색 및 지원
- 이력서 관리 (프로필 사진, 건강검진증 업로드)
- 근무 배정 확인 및 출근 체크 (GPS 지오펜싱)
- 근무내역/급여 확인
- 계약서 전자서명 (PDF 생성)
- 푸시 알림 수신 (FCM)

### 관리자(파견업체) 기능
- 고객사(파견처) 관리 (위치 좌표, 현장 사진)
- 채용공고 등록/관리 (시급제/기간제)
- 지원자 승인/거절
- 근무표 관리 (배정 등록/수정/삭제, 드래그앤드롭)
- 출근 현황 실시간 모니터링 (ON/OFF/출근/노쇼/이탈)
- FCM 푸시 알림 발송 (자동/수동)
- 급여 관리 (Google Sheets 연동, 급여 계산)
- 계약서 관리 (전자서명 확인)
- 파트너 제휴 문의 관리

### 시스템 기능
- GPS 지오펜싱 출근 확인 (5km 접근 → 2km 근접 → 300m 도착 → 500m 이탈 감지)
- 3레이어 안전장치 (OS 네이티브 지오펜스 + Silent Push + JS 포그라운드 추적)
- GPS 튐 방어 (3회 연속 검증 arrive debounce)
- API Key 영구 인증 (Supabase 토큰 만료 회피)
- FCM 푸시 알림 시스템 (배정/출근/노쇼 자동 알림, Silent Push 포함)
- Cron 기반 출근 체크 (5분 간격, 최대 3회 알림 후 노쇼 처리)
- 이탈 추적 1시간 제한 (근무 중 외출 노이즈 제거)
- RLS(Row Level Security) 기반 데이터 접근 제어
- 웹/앱 오버레이 빌드 시스템 (app-native → app 덮어쓰기)

---

## 3) 주요 업무

### 프론트엔드
- Next.js 16 App Router 기반 웹 애플리케이션 개발
- 서버 컴포넌트 / 클라이언트 컴포넌트 분리 설계
- Server Actions를 활용한 데이터 뮤테이션
- shadcn/ui 기반 반응형 UI 구현
- 관리자 대시보드 (근무표, 급여, 계약서 등)

### 백엔드
- Supabase PostgreSQL 스키마 설계 (12+ 테이블)
- RLS 정책 설계 (회원은 본인 데이터만, 관리자는 전체 접근)
- API Routes 개발 (인증, 출근, 푸시, 파일 업로드 등)
- Cron Job 구현 (출근 알림 자동 발송, 노쇼 처리)
- Google Sheets API 연동 (급여 데이터 Import)

### 모바일 앱
- Capacitor 8 기반 Android/iOS 하이브리드 앱 개발
- 웹/앱 오버레이 패턴 설계 (app-native 분리)
- GPS 지오펜싱 출근 확인 시스템 구축
- 백그라운드 위치 추적 (background-geolocation 플러그인)
- FCM 푸시 알림 (토큰 관리, 채널 설정)
- Google/Apple 네이티브 소셜 로그인
- 앱 빌드 자동화 스크립트 (8단계 빌드 파이프라인)

### 인프라/배포
- Vercel 프로덕션 배포 (웹)
- Android APK/AAB 빌드 및 Google Play 배포
- iOS Archive 및 TestFlight/App Store 배포
- Supabase 마이그레이션 관리 (40+ 마이그레이션)

---

## 4) 주안점

### 지오펜싱 출근 확인 시스템
인력파견 업무의 핵심인 **출근 확인을 자동화**했습니다. 실시간 위치 추적이 아닌 이벤트 기반 지오펜싱으로 배터리 소모를 최소화하면서 정확한 출근/이탈 기록을 남깁니다. **5km 접근 → 2km 근접 → 300m 도착 → 500m 이탈**의 4단계 감지와, 하루 다수 근무의 순차 처리를 지원합니다.

**3레이어 안전장치**: 각 반경(5km/2km/300m)마다 OS 네이티브 지오펜스(Layer 1), Silent Push(Layer 2), JS 포그라운드(Layer 3)가 독립적으로 동작하여 하나 실패해도 다른 레이어가 보완. 실내 GPS 대응을 위한 GPS Fallback Chain(getLastLocation → Balanced → HighAccuracy 순차 시도)과 GPS 튐 방어를 위한 **arrive 3회 연속 검증**으로 허위 출근을 차단합니다.

### 웹/앱 코드 통합 관리
하나의 코드베이스로 웹과 모바일 앱을 동시에 관리합니다. `app-native/` 오버레이 패턴으로 앱 전용 UI/로직을 분리하고, 빌드 스크립트가 자동으로 합성합니다. Server Actions는 웹에서만 동작하므로 앱용 API 레이어(`native-api/`)를 별도로 구성했습니다.

### 보안 설계
Supabase RLS로 데이터베이스 레벨에서 접근 제어를 적용했습니다. 회원은 본인 데이터만 조회/수정 가능하고, 관리자는 admins 테이블 존재 여부로 권한을 검증합니다. 서비스 키는 서버 전용으로 분리하고, 클라이언트에서는 anon key + RLS로만 접근합니다.

### 관리자 중심 운영 도구
파견업체 실무자가 바로 사용할 수 있는 **운영 중심 관리자 페이지**를 구현했습니다. 근무표에서 배정/알림/출근 상태를 한 화면에서 확인하고, FCM 수동 발송, 급여 일괄 처리, 계약서 전자서명 관리 등 실무 업무를 효율화했습니다.

### 점진적 권한 요청
설치 시 모든 권한을 한꺼번에 요청하지 않고, 필요한 시점에 단계적으로 요청합니다. 회원가입 시 기본 위치 권한, 지오펜싱 시작 시 백그라운드 권한, 권한 부족 시 설정 화면 유도로 사용자 거부감을 최소화했습니다.
