# 휴멘드 HR 지오펜싱 출근 확인 시스템

## 개요

회원(파견 근로자)의 출근 여부를 GPS 기반 지오펜싱으로 자동 확인하는 시스템입니다.
실시간 위치 추적이 아닌 **이벤트 기반** 방식으로, 근무지 접근/도착/이탈 시에만 서버에 기록합니다.

---

## 지오펜싱 반경

| 구분 | 반경 | 동작 |
|------|------|------|
| 접근 감지 | 5km | 1회 감지, `approaching_at` 기록 |
| 근접 감지 | 2km | 1회 감지, `nearby_at` 기록 |
| 출근 확인 | 300m | `arrived` 처리, `arrived_at` 기록 (서버 측 3회 연속 검증) |
| 이탈 감지 | 500m | 출근 후 1시간 이내만 추적, `departure_logs` 기록 |
| 위치 정확도 필터 (5km) | 500m 이하 | 이동 중 느슨한 감지 |
| 위치 정확도 필터 (2km) | 400m 이하 | 실내 수용 |
| 위치 정확도 필터 (300m 출근) | 300m 이하 | 실내 감지 가능 (서버 이중 검증) |
| 위치 업데이트 간격 | 50m | 정지 상태에서도 업데이트 빈도 증가 |

---

## 3레이어 × 3반경 = **9단 안전장치**

각 반경(5km/2km/300m)마다 **3개의 독립 레이어**로 감지. 하나 실패해도 다른 레이어가 보완.

### Layer 1: **OS 네이티브 지오펜스**
- 앱 실행 상태와 무관하게 OS가 감시
- 앱이 꺼져 있어도 ENTER 이벤트 시 OS가 앱 깨움
- iOS: `CLLocationManager.startMonitoring(for:)` / Android: Google Play Services Geofencing

| 반경 | iOS Identifier | Android Identifier |
|:-:|:-:|:-:|
| 5km | `approach_{shiftId}` | `approach_{shiftId}` |
| 2km | `shift_{shiftId}` | `shift_{shiftId}` |
| 500m (출근) | `arrive_{shiftId}` | `arrive_{shiftId}` |
| 500m (이탈 EXIT) | `depart_{shiftId}` | `depart_{shiftId}` |

### Layer 2: **Silent Push** (서버 Cron 2층)
- 매 5분마다 `arrived_at` null인 shift 조회
- data-only FCM 발송 → 앱 백그라운드에서 깨어남
- 네이티브 GPS 측정 → 거리별 API 호출
  - ≤ 300m → nearby + arrive
  - ≤ 2km → nearby
  - ≤ 5km → approaching

### Layer 3: **JS 포그라운드 추적** (background-geolocation)
- 앱 실행 중 또는 "항상 허용" 권한 상태에서 동작
- `distanceFilter: 50m`로 이동마다 체크
- `@capacitor-community/background-geolocation` 플러그인

### 3레이어 × 3반경 매트릭스

| 반경 | Layer 1 (OS) | Layer 2 (Silent Push) | Layer 3 (JS) |
|:-:|:-:|:-:|:-:|
| 5km 접근 | ✅ | ✅ | ✅ |
| 2km 근접 | ✅ | ✅ | ✅ |
| 300m 출근 | ✅ | ✅ | ✅ |

---

## 이벤트 흐름

### 1단계: 5km 접근 감지 (1회)
```
회원이 근무지 5km 이내 진입
-> OS 지오펜스 ENTER 또는 Silent Push 또는 JS 포그라운드
-> /api/native/attendance/approaching API 호출
-> daily_shifts.approaching_at에 시간 기록
-> 관리자 페이지에 "5km HH:MM" 표시 (보라색)
```

### 2단계: 2km 근접 감지 (1회)
```
회원이 근무지 2km 이내 진입
-> 3레이어 중 하나가 감지
-> /api/native/attendance/nearby API 호출
-> daily_shifts.nearby_at에 시간 기록
-> 관리자 페이지에 "2km HH:MM" 표시 (파란색)
```

### 3단계: 300m 출근 확인 (3회 연속 검증)
```
회원이 근무지 300m 이내 도착
-> /api/native/attendance/arrive API 호출 (5초 이상 간격)
-> 서버 측 haversine 거리 재검증 (300m 초과 시 거부)
-> 3회 연속 300m 이내 확인되면 arrived 확정
-> daily_shifts.arrival_status = "arrived"
-> daily_shifts.arrived_at에 시간 기록
-> 관리자 페이지에 "출근 HH:MM" 표시 (초록색)
-> 이후 이탈 감지 모드로 자동 전환
```

**GPS 튐 방어 — arrive Debounce (047 마이그레이션)**:
- `arrive_attempts` 컬럼으로 연속 확인 카운팅
- `last_arrive_attempt_at` 타임스탬프
- `last_arrive_lat/lng`로 이전 샘플과 500m 이상 drift 감지 시 리셋
- 5초 미만 간격 호출은 스킵 (MIN_INTERVAL)
- 10분 초과 간격 시 카운터 리셋 (MAX_INTERVAL)

### 4단계: 500m 이탈 감지 (출근 후 1시간만)
```
회원이 근무지 500m 밖으로 이동 (2회 연속 감지 시 확정)
-> /api/native/attendance/depart API 호출
-> 서버: 출근 시간 + 1시간 이내만 기록 (이후는 "tracking_ended" 응답)
-> departure_logs에 이탈 시간 + 좌표 기록
-> 관리자 페이지에 "이탈 중" 배지 표시 (주황색, 깜빡임)
```

### 5단계: 500m 이내 복귀 (1시간 이내만)
```
이탈 중 근무지 500m 이내로 복귀
-> /api/native/attendance/return API 호출
-> departure_logs.returned_at에 시간 기록
-> departure_logs.duration_minutes 계산
-> 관리자 페이지에 "이탈 N회, 총 N분, HH:MM~HH:MM" 표시
```

**이탈 추적 1시간 제한 이유**:
- 근무 중 외출(배달/심부름/교대 등) 노이즈 제거
- 배터리 절약 — 1시간 경과 시 watcher 자동 중단

---

## 인증 시스템 — API Key (영구)

### 문제: Supabase Access Token 1시간 만료
- 백그라운드에서 OS 지오펜스 → API 호출 시 토큰 만료되어 401
- 네이티브 코드에서 토큰 갱신 로직 복잡

### 해결: members.api_key UUID
- 회원가입 시 자동 생성 (046 마이그레이션)
- **만료 없음**
- 네이티브 SharedPreferences/UserDefaults에 저장
- attendance API 호출 시 `X-API-Key` 헤더 우선 사용
- Fallback: Bearer 토큰

### 인증 흐름
```
1. 앱 실행 → today API 호출 → 응답에 apiKey 포함
2. setNativeApiKey(apiKey) → 네이티브 저장소 저장
3. 이후 모든 attendance API 호출 시 X-API-Key 헤더 사용
4. 서버 authenticateMember() — API Key 우선, Bearer fallback
```

---

## GPS Fallback Chain (실내 감지 개선)

### 3단 재시도 (Silent Push 처리 시)

**Android (`LocationCheckFirebaseService.java`)**:
1. `getLastLocation()` — 3분 이내 캐시, 즉시 반환
2. `PRIORITY_BALANCED_POWER_ACCURACY` — WiFi/셀룰러, 5초
3. `PRIORITY_HIGH_ACCURACY` — GPS, 10초

**iOS (`LocationChecker`, `ArriveLocationHelper`)**:
- `kCLLocationAccuracyHundredMeters` (WiFi/셀룰러 활용)
- 10초 타임아웃 × 3회 재시도
- 정확도 500m 이하만 수용

실내에서 GPS 정확도 100m 초과해도 WiFi 기반 위치로 보완 → 감지 성공률 향상.

---

## iOS 위치 권한 "항상 허용" 자동 유도

### Apple 정책 한계
iOS 13+ 이후 **첫 팝업에 "항상 허용" 옵션 불가**. 강제로 2단계 플로우.

### 현재 구현 (NativeGeofencePlugin.swift)
- `requestAlwaysAuthorization()` 직접 호출
- `notDetermined`: 사전 안내 alert → iOS 1차 팝업 → 2차 Always 팝업 자동 표시
- `whenInUse`: Always 업그레이드 팝업 즉시 표시
- `denied`: 설정 앱으로 유도

### 사용자 안내 플로우
```
[앱 내 사전 alert]
"팝업이 2번 연속으로 뜹니다:
 1번째: 앱을 사용하는 동안 허용
 2번째: 항상 허용
 ⚠️ 한 번 허용 선택 시 출근 확인 불가"
     ↓
[iOS 1차 팝업]
 한 번 허용 / 앱 사용 중 허용 / 허용 안 함
     ↓ (앱 사용 중 선택)
[iOS 2차 팝업 자동 표시]
 항상 허용 / 앱 사용 중에만 유지
     ↓ (항상 허용 선택)
 완료 ✅
```

---

## FCM Silent Push 개선

### iOS apns-priority 수정 (`fcm.ts`)
- **이전**: `apns-priority: "10"` (silent push에 잘못된 값)
- **변경**: `apns-priority: "5"` (Apple 공식 silent push 우선순위)
- 효과: iOS가 Silent Push 정상 전달 (이전엔 거부 가능)

### 추가 설정
- `apns-push-type: "background"` — 명시적 background push
- Android: `priority: "high"`, `ttl: "600s"`, `collapse_key` 설정
- Doze 모드 우회 + 중복 발송 방지

---

## 감지 실패 보완: Silent Push 반복 발송

### Cron 조건 (출근 확정까지 반복)
```
매 5분마다 실행:
  arrived_at IS NULL AND arrival_status IN ('pending', 'notified', 'confirmed')
  AND 출근 90분 전 ~ 출근 후 180분 구간
  → Silent Push 발송
```

### 발송 범위
- **시작**: 출근 90분 전
- **종료**: 출근 후 180분 (지각 대비 연장)
- `arrived_at` 기록되면 즉시 발송 중단

### 전체 보완 체계

| 상황 | 대응 |
|------|------|
| 앱 안 열림 | Cron이 15분마다 재알림 FCM 발송 (최대 3회) |
| 앱 열었다 껐음 | 다시 열면 `appStateChange` -> 지오펜싱 재시작 |
| 5km/2km/300m 미감지 | OS 네이티브 지오펜스 또는 Silent Push로 재시도 |
| GPS 튐 (허위 arrive) | 서버 3회 연속 검증으로 차단 |
| 전부 놓침 | 출근 시간 경과 + 3회 미응답 -> 노쇼 처리 + 관리자 알림 |

---

## 다수 근무 순차 처리

하루에 여러 근무가 배정된 경우, today API가 **미완료 근무 중 가장 빠른 것**을 반환합니다.

```
08:00 근무 pending -> today API가 08:00 반환 -> 지오펜싱 시작
08:00 근무 arrived -> 앱 재시작 -> today API가 16:00 반환 -> 16:00 지오펜싱 시작
16:00 근무 arrived -> 앱 재시작 -> today API가 20:00 반환 -> 20:00 지오펜싱 시작
모두 arrived -> 마지막 arrived 반환 -> 이탈 감지
```

**주의**: 근무 완료 후 다음 근무로 전환하려면 **앱을 껐다 켜야** 합니다.

---

## 위치 권한

### OS 팝업 옵션 (앱에서 제어 불가)

**Android 팝업 옵션:**
- "앱 사용 중에만 허용"
- "허용 안함"
- (항상 허용 옵션 없음 -- 설정에서만 변경 가능)

**iOS 팝업 옵션:**
- "한 번 허용"
- "앱 사용 중 허용"
- "허용 안 함"
- (항상 허용 옵션 없음 -- 앱이 `requestAlwaysAuthorization()` 호출 시 업그레이드 팝업)

### 위치 권한별 동작

| 위치 권한 | 포그라운드 | 백그라운드 |
|---|---|---|
| 항상 허용 | O | O |
| 앱 사용 중에만 (Android, FG Service 상주 시) | O | O |
| 앱 사용 중에만 (iOS) | O | X |
| 한번 허용 (iOS) | O (1회, 앱 닫으면 사라짐) | X |
| 허용 안함 | X | X |

**iOS는 "항상 허용" 필수**. Android는 FG Service로 "앱 사용 중"이어도 가능.

---

## FCM 출근 알림 (일반 푸시)

지오펜싱과 별도로, 서버 cron이 출근 알림을 발송합니다.

### 알림 발송 조건
- `alert_minutes_before` (기본 60분) 전부터 발송 시작
- `alert_interval_minutes` (기본 15분) 간격으로 반복
- `alert_max_count` (기본 3회)까지 발송
- 출근시간 경과 + 최대 횟수 초과 -> **noshow 처리**
- 재알림 간격 체크에 `-1분 버퍼` 적용 (서브초 타이밍 차이 방지)

### 알림 발송 기록
- `notification_logs` 테이블에 `shift_id`와 함께 저장
- 관리자 페이지에서 **근무지별로 구분**되어 표시
- 자동/수동 구분 (trigger_type: auto/manual)

### 근무 시간 변경 시
- `notification_sent_count` 자동 리셋
- `arrival_status` -> pending으로 초기화
- 알림이 처음부터 다시 발송됨

---

## 관리자 페이지 표시 항목

| 항목 | 색상 | 의미 |
|------|------|------|
| OFF | 빨간색 | pending -- 알림 미응답 |
| ON | 초록색 | confirmed -- 출근하겠다고 응답 |
| 5km HH:MM | 보라색 | 5km 이내 접근 시간 |
| 2km HH:MM | 파란색 | 2km 이내 근접 시간 |
| 출근 HH:MM | 초록색 | 300m 이내 도착 시간 (3회 검증 후) |
| 이탈 중 | 주황색 (깜빡임) | 출근 후 500m 밖으로 이탈 |
| 이탈 N회, 총 N분 | 주황색 | 이탈 횟수와 총 이탈 시간 |
| HH:MM~HH:MM | 회색 | 이탈 시작~복귀 시간 |
| 노쇼 | 빨간색 | 알림 최대 횟수 초과 + 출근시간 경과 |

---

## 실시간 위치 추적 관련

- **현재 시스템은 실시간 위치를 저장하지 않음**
- 위치 판단은 앱(디바이스) 내부에서 수행
- 서버에는 이벤트 결과(접근/근접/도착/이탈)만 전송
- 관리자가 회원의 현재 위치를 확인하는 기능은 없음
- 이탈 기록의 `departed_lat`, `departed_lng`에 이탈 시점의 좌표만 저장

---

## 출근 체크 타임라인 예시 (09:00 출근)

| 시각 | 이벤트 |
|------|--------|
| ~07:30 | Cron -> "출근 예정이신가요?" FCM 발송 (60분 전) |
| ~07:30 | 회원 알림 터치 -> 앱 실행 -> 지오펜싱 시작 |
| 이동 중 | 5km 진입 (OS 지오펜스 ENTER 또는 JS) -> `approaching_at` 기록 |
| 이동 중 | 2km 진입 -> `nearby_at` 기록 |
| 접근 | 300m 진입 시도 #1 -> 서버 거리 검증 통과 -> `arrive_attempts=1` |
| 접근 | 300m 진입 시도 #2 (5초 후) -> `arrive_attempts=2` |
| 도착 | 300m 진입 시도 #3 -> 3회 연속 통과 -> `arrived` 확정 |
| --- | --- 미응답 시 --- |
| 07:45 | Cron -> 재알림 1회 (15분 경과) |
| 08:00 | Cron -> 재알림 2회 |
| 08:15 | Cron -> 재알림 3회 (최대) |
| 09:00 | 3회 소진 + 시간 경과 -> **노쇼 처리** + 관리자 알림 |
| --- | --- 감지 실패 시 --- |
| 매 5분 | Cron -> `arrived_at` null 확인 -> Silent Push 발송 -> 출근 후 180분까지 반복 |
| --- | --- GPS 튐 발생 시 --- |
| 임의 시점 | 1회 arrive 시도 -> count=1 -> 다음 샘플에서 500m drift 감지 -> 카운터 리셋 |

---

## 주요 코드 위치

### 공통
| 파일 | 역할 |
|------|------|
| `src/lib/capacitor/geofence.ts` | JS 지오펜싱 핵심 로직 (5km/2km/300m/500m 반경) |
| `src/hooks/useAttendance.ts` | 앱 시작 시 지오펜싱 초기화 + iOS 권한 2단계 자동화 |
| `src/lib/capacitor/push.ts` | FCM 수신 핸들러 (Silent Push -> 지오펜싱 재시작) |
| `src/lib/capacitor/native-geofence.ts` | OS 지오펜스 등록 + API Key 저장 |

### 서버 API
| 파일 | 역할 |
|------|------|
| `src/lib/supabase/member-auth.ts` | `authenticateMember()` — API Key + Bearer 인증 |
| `src/app/api/native/attendance/today/route.ts` | 오늘 근무 조회 + apiKey 반환 |
| `src/app/api/native/attendance/approaching/route.ts` | 5km 접근 기록 |
| `src/app/api/native/attendance/nearby/route.ts` | 2km 근접 기록 |
| `src/app/api/native/attendance/arrive/route.ts` | 300m 도착 기록 (3회 debounce) |
| `src/app/api/native/attendance/depart/route.ts` | 이탈 기록 (1시간 제한) |
| `src/app/api/native/attendance/return/route.ts` | 복귀 기록 |
| `src/app/api/cron/attendance-check/route.ts` | 5분 cron — FCM 알림 + Silent Push |
| `src/lib/push/fcm.ts` | FCM 발송 (apns-priority 5 silent push) |
| `src/lib/push/attendance-notify.ts` | 알림 발송 헬퍼 |

### iOS 네이티브
| 파일 | 역할 |
|------|------|
| `ios/App/App/AppDelegate.swift` | FCM 수신 → 5km/2km/500m region 등록 + `didEnterRegion`/`didDetermineState` + `LocationChecker`/`ArriveLocationHelper` (GPS Fallback) |
| `ios/App/App/NativeGeofencePlugin.swift` | register/setApiKey/requestAlwaysAuthorization/getAuthorizationStatus |

### Android 네이티브
| 파일 | 역할 |
|------|------|
| `android/.../NativeGeofencePlugin.java` | 지오펜스 등록 + API Key + 배터리 최적화 예외 + WorkManager |
| `android/.../GeofenceBroadcastReceiver.java` | OS 지오펜스 ENTER/EXIT → approaching/nearby/arrive/depart API |
| `android/.../LocationCheckFirebaseService.java` | Silent Push 수신 → Foreground Service → GPS Fallback → API |
| `android/.../LocationBackupWorker.java` | WorkManager 15분 주기 백업 체크 |

### DB 마이그레이션
| 번호 | 내용 |
|:-:|------|
| 044 | `approaching_at` 컬럼 추가 |
| 046 | `members.api_key` UUID 컬럼 추가 (영구 인증) |
| 047 | `arrive_attempts`, `last_arrive_attempt_at`, `last_arrive_lat/lng` (3회 debounce) |

---

## 해결된 이슈

### 1. today API `.single()` 문제
- **문제**: 하루에 여러 근무 배정 시 `.single()`이 에러 반환 -> shift: null
- **해결**: 전체 조회 후 미완료 근무 우선 반환

### 2. FCM 발송 기록 근무지 미구분
- **문제**: `notification_logs`에 `shift_id` 없어서 모든 근무지에 동일한 발송 기록 표시
- **해결**: `shift_id` 컬럼 추가

### 3. 위치 권한 미요청
- **문제**: `requestPermissions: false`로 설정되어 OS 팝업이 안 뜸
- **해결**: `requestPermissions: true` + 권한 부족 시 설정 화면 유도

### 4. 근무 시간 변경 시 알림 카운트 미리셋
- **해결**: `updateShiftGroup`에서 시간 변경 시 카운트 + 상태 자동 리셋

### 5. 빌드 스크립트 소스 복원 문제
- **해결**: 수정 사항을 git 커밋하여 복원 후에도 유지

### 6. 5km/2km 감지 실패 보완 없음
- **해결**: Cron에서 arrived_at null인 동안 계속 Silent Push 발송

### 7. **5km OS 네이티브 지오펜스 누락** (2026-04-19 발견)
- **문제**: 2km/500m만 네이티브 등록, 5km은 JS 레이어에만 존재 → 앱 꺼지면 5km 감지 불가
- **해결**: iOS `approach_{shiftId}` region + Android `approach_` 접두사 처리 추가

### 8. **arrive 1회 샘플로 즉시 판정 → GPS 튐으로 허위 출근** (2026-04-19 발견)
- **문제**: 정기훈 07:58 허위 arrive → 실제 도착은 08:20
- **해결**: 서버 3회 연속 검증 debounce 도입 (047 마이그레이션)

### 9. **iOS Silent Push 전달 안 됨** (2026-04-19 발견)
- **문제**: `apns-priority: 10` + `content-available: 1` = Apple 규정 위반 → 전달 거부
- **해결**: `apns-priority: 5` + `apns-push-type: background`

### 10. **iOS 권한 "항상 허용" 팝업 안 뜸** (2026-04-19 발견)
- **문제**: Apple 정책상 첫 팝업에 "항상 허용" 불가
- **해결**: `requestAlwaysAuthorization()` 직접 호출 + 사전 안내 alert → 2단계 자동 플로우

### 11. **Supabase Access Token 1시간 만료** (2026-04-19 발견)
- **문제**: 백그라운드 지오펜스 감지 → 만료된 토큰으로 API 호출 → 401
- **해결**: `members.api_key` UUID 영구 인증 (X-API-Key 헤더)

### 12. **이탈 추적이 근무 끝까지 계속** (2026-04-19 발견)
- **문제**: 근무 중 외출(배달/심부름)도 이탈로 기록
- **해결**: 출근 후 1시간만 이탈 추적 (서버 + 앱 모두)

### 13. **실내 GPS 정확도 부족으로 감지 실패** (2026-04-19 발견)
- **문제**: 정확도 100m 이상이면 모든 위치 데이터 필터링 → 실내 감지 불가
- **해결**: 레이어별 정확도 완화 (5km=500m, 2km=400m, 300m=300m) + GPS Fallback Chain
